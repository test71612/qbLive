# كيف تعمل اللوحة (شرح مختصر لغير المبرمجين)

```
متصفحك ──► صفحات Next.js ──► مسارات /api (على الخادم)
                                   │
                                   ├──► GitHub API : يقرأ شجرة الملفات ومحتواها والـ commits
                                   └──► Supabase   : يخزّن الحجوزات والملاحظات والمهام والسجل
                                          │
متصفحك ◄────── تحديث مباشر ◄──────────────┘  (Supabase Realtime)
```

**النقطة المهمة:** اللوحة لا تكتب شيفرة ولا تدفع commits. هي طبقة تنسيق فوق المستودع.

## دورة حياة الحجز

1. تختار ملفًا أو أكثر وتضغط «احجز».
2. تكتب سببًا بجملة واحدة (إجباري — بلا سبب لا فائدة من الحجز).
3. إن كان أحدهم حاجزًا نفس الملف، تظهر لك بطاقته باسمه وسببه قبل أن تكمل.
4. تظهر بطاقة حجزك فورًا عند كل زميل مفتوحة عنده اللوحة.
5. تُنهي الحجز يدويًا، أو ينتهي تلقائيًا بعد ٣ ساعات بلا نشاط (`LOCK_IDLE_HOURS`).
6. كل خطوة تُسجَّل في `audit_log`، فسؤال «من فتح هذا الملف آخر مرة؟» له جواب.

## الجداول

| الجدول | الغرض |
|---|---|
| `app_users` | من دخل، وما دوره (`admin` / `member`) |
| `repos` | المستودعات المتصلة |
| `file_locks` | قلب الأداة: من يحجز أي ملفات ولماذا |
| `file_notes` | سطر بلغة بسيطة يشرح دور كل ملف |
| `tasks` | لوحة المهام المربوطة بنفس مسارات الملفات |
| `dep_graphs` | رسم الاستدعاءات بين الملفات (يولّده madge) |
| `related_files` | روابط يدوية للملفات التي لا يراها الرسم (صور، SQL، إعدادات) |
| `map_overrides` | أسماء ودّية للعُقد في الخريطة |
| `audit_log` | كل حدث: حجز، فك، فك إداري، push |

## التوافق مع المرحلة الثانية

اللوحة مستودع منفصل تمامًا عن المنتج، بنفس المكدّس (Next.js + TypeScript + Tailwind + Supabase) حتى يكون ما يتعلّمه الفريق هنا قابلًا لإعادة الاستخدام هناك. لا شيء في هذه الشيفرة يفترض شكل المنتج، فتضيف عليه وحدات الألعاب والبطولات لاحقًا دون أي ارتباط بهذه الأداة.
